---
const { src = "", title = null } = Astro.props;
---

<div class="audio-container">
    {title && <h4 id="audioTitle">{title}</h4>}
    
    <audio controls>
        <source src={src} type="audio/mpeg">
    </audio>
</div>

<style>
    .audio-container {
        background: #1a1c22;
        padding: 15px; /* Aumentato un filo per dare respiro */
        border-radius: 12px;
        border: 1px solid #997800;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 5px; /* Spazio tra titolo e player */
        max-width: 350px;
        margin: 20px auto;
        box-shadow: 0 4px 15px rgb(0, 0, 0);
    }

    #audioTitle {
        text-align: center;
        margin: 0; /* AZZERA IL MARGINE DI DEFAULT */
        margin-bottom: 5px; /* Opzionale: un piccolissimo distacco dal player */
        color: #997800; /* Per richiamare il bordo */
        font-family: serif; /* Per lo stile elegante della foto */
        text-transform: uppercase;
    }

    /* Rende il player audio un po' più stretto per stare nel box */
    audio {
        width: 100%;
        height: 40px;
    }
</style>